class Floor {

    constructor(floorNumber) {
        this.floorNumber = floorNumber;
    }

    spawnDoors() {
        if (this.floorNumber = 1 || numberOfFloors) {
            // spawn central door, outer doors, 2 trap doors
        } else if ((this.floorNumber % 2) == 1) {
            // spawn outer doors, 2 trap doors
        } else {
            // spawn outer doors, 1 trap door
        }
    }

}

export default Floor;